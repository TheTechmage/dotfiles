pacstrap /mnt base vim xorg-server{,-utils} slim syslinux xfce4 subtle git zsh libvirt xf86-video-{intel,nouveau} tmux screen openssh
pacstrap /mnt htop lsof samba ncdu tree nfs-utils sudo python virtualbox abs base-devel chromium xscreensaver
echo -e 'LANG="en_US.UTF-8"\nLC_COLLATE="C"'>> /mnt/etc/locale.conf
sed -i 's/^#\(en_US\|ja_JP\)/\1/' /mnt/etc/locale.gen
archchroot /mnt
	mkinitcpio -p linux
	locale-gen
	ln -s /usr/share/zoneinfo/America/Denver /etc/localtime
	useradd -m -G wheel,systemd-journal,fuse,mpd,vboxusers,libvirt -s /bin/zsh frosty
	su - frosty
		git clone https://github.com/frostyfrog/dotfiles.git .files
		cd .files
		git submodule update --init --recursive
		./autocopy
	systemctl enable libvirtd sshd slim mpd


# vim: set ai ft=sh syntax=sh :
